import { Container, Row } from 'react-bootstrap';

import styles from './game.screen.module.scss';

export const GameScreen = () => {
  return <Container className={styles.game} fluid></Container>;
};
